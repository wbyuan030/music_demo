// import { useState } from 'react'
// import 'App.css'
import { useState, type ReactNode } from "react"

interface MainpageProps {
  sideBar: ReactNode;
  mainContent: ReactNode;
  top: ReactNode;
  bottom: ReactNode;
}

function Mainpage({ sideBar, mainContent, top, bottom }: MainpageProps) {
  const [showSideBar, setSideBar] = useState(false)
  return (
    <>
      <div className="flex flex-col h-screen  ">
        <div className="h-2/12 mt-2">
          {top}
        </div>
        <div className="flex flex-1 flex-row gap-4 min-h-0">

          {/* 侧边 */}

          <div className="w-3/10  bg-neutral-50 rounded-xl">
            {sideBar}
          </div>
          <div className="flex flex-1 bg-neutral-900 rounded-xl overflow-y-auto p-4">
            {mainContent}
          </div>
        </div>
        <div className="h-1/12">
          {bottom}
        </div>
      </div>
    </>
  )
}

export default Mainpage
